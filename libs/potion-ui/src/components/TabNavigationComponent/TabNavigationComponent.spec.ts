import { describe, expect, it } from "vitest";

import TabNavigationComponent from "./TabNavigationComponent.vue";

describe("TabNavigationComponent", () => {
  it("compiles properly", () => {
    expect(TabNavigationComponent).toBeTruthy();
  });
});
