import { describe, it, expect } from "vitest";

import InlineMenu from "./InlineMenu.vue";

describe("InlineMenu", () => {
  it("compiles properly", () => {
    expect(InlineMenu).toBeTruthy();
  });
});
