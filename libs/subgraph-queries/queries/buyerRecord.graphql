query getBuyerRecords($buyer: Bytes, $expiry: BigInt) {
  buyerRecords(where: { buyer: $buyer, expiry: $expiry }) {
    id
    buyer
    expiry
    numberOfOTokens
    otoken {
      expiry
      id
      isPut
      premium
      strikePrice
    }
  }
}
